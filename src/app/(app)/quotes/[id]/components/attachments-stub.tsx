// Placeholder. Day 6 wires Google Picker (drive.file) + quote_attachments writes.

export function AttachmentsStub() {
  return (
    <div className="rounded-md border border-dashed bg-muted/30 px-4 py-3 text-sm text-muted-foreground">
      <span className="font-medium text-foreground">Drive attachments</span>{" "}
      land on Day 6 — Google Picker integration with{" "}
      <code className="text-xs">drive.file</code> scope.
    </div>
  );
}
